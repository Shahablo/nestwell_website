{
  "name": "nestwell-website",
  "private": true,
  "version": "0.1.0",
  "description": "NestWell marketing website — static, Vite-built, deployed to GitHub Pages",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.0"
  }
}
